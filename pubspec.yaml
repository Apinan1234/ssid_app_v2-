name: ssid_app_v2
description: "Surgical Skill Improvement Dashboard - AI-powered suturing analysis app"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  
  # UI & Icons
  cupertino_icons: ^1.0.8
  lucide_icons: ^0.257.0
  google_fonts: ^7.1.0
  
  # State Management
  provider: ^6.1.2
  
  # Firebase
  firebase_core: ^3.15.2
  cloud_firestore: ^5.6.12
  firebase_auth: ^5.7.0
  
  # Media & Camera
  camera: ^0.11.0+2
  image_picker: ^1.1.2
  video_player: ^2.10.1
  path_provider: ^2.1.5
  
  # Local Storage
  shared_preferences: ^2.3.4
  sqflite: ^2.4.2
  path: ^1.9.1
  
  # Utilities
  intl: ^0.20.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  
  # assets:
  #   - assets/
